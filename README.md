# Xiangchen Wang Academic Homepage

This repository contains an Academic Pages-based personal homepage for Xiangchen Wang.

## Local Preview

```bash
bundle install
bundle exec jekyll serve -l -H localhost
```

Then open <http://localhost:4000>.
